from .config import REDIS_DB, REDIS_HOST, REDIS_PORT

# Settings System Tickets
settingsTicketSystem = dict(
  name="",
)

# Setup Logging ||| System Tickets


# Settings Redis

redis_config = {
    'host': REDIS_HOST,
    'port': REDIS_PORT,
    'db': REDIS_DB
}

redis_settings = {
  'set1': True
}